<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
      <TargetFramework>netstandard2.0</TargetFramework>
	  <LangVersion>latest</LangVersion>
	  <Configurations>Debug;Release;local</Configurations>
	  <Title>EDDY Edifact Parser</Title>
	  <Authors>GotWoods</Authors>
	  <Version>0.0.6-beta</Version>
	  <Description>Parser for the Edifact standard into POCO objects</Description>
  </PropertyGroup>

  <ItemGroup>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
      <_Parameter1>Eddy.Edifact.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>

  <ItemGroup>
    <!-- The published Eddy.Core nuget package (0.0.9-beta, restored in this environment) predates
         SegmentSource/ISourceTracked and the new Error/ValidationResult fields this project needs.
         Reference the sibling project directly so we build against current Eddy.Core sources. -->
    <ProjectReference Include="..\Eddy.Core\Eddy.Core.csproj" />
  </ItemGroup>

</Project>
